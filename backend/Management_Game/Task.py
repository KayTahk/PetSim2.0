class Task:
    def __init__(self, progress, progress_needed, difficulty, skillset, stat1, stat2, condition):
        self.progress = progress
        self.progress_needed = progress
        self.difficulty = difficulty
        self.skillset = skillset
        self.stat1 = stat1
        self.stat2 = stat2
        self.condition = condition
        